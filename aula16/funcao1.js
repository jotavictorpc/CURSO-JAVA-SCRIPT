function parimp(n) {         // Parâmetro 
    if (n % 2 == 0) {
        return "Par"
    }else {                    // Ação
        return "Impar"           // Retorno
    }
}

console.log(parimp(223)) // Chamada
